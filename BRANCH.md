# Branch: exp/fly-plan-point

This branch experiments with wrapping flight plan waypoints in a structured
`FlightPlanPoint` data class.

Status: experimental. The main UI layout still needs validation around flight
plan hint text, so this branch should not be merged into `main` until the
status display behavior is stable.
